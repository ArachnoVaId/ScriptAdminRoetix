# Concert — Engine Code & Pola Script

## Purpose

Dokumen ini berisi engine code patterns yang digunakan di semua script Concert.
Sama untuk semua file — cukup salin verbatim.

---

## 1. Utility Functions (Gunakan di semua script)

```javascript
const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value')?.set;
const taSetter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value')?.set;

function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

function fill(el, val) {
  if (!el) return console.warn('⚠ NOT FOUND:', val);
  el.focus();
  if (el.tagName === 'TEXTAREA' && taSetter) taSetter.call(el, val);
  else if (el.tagName === 'SELECT') { el.value = val; }
  else if (setter) setter.call(el, val);
  else el.value = val;
  el.dispatchEvent(new Event('input', { bubbles: true }));
  el.dispatchEvent(new Event('change', { bubbles: true }));
}
```

Untuk script yang hanya butuh input (tanpa textarea/select), cukup:

```javascript
const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value')?.set;

function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

function fill(el, val) {
  if (!el) return;
  el.focus();
  if (setter) setter.call(el, val);
  else el.value = val;
  el.dispatchEvent(new Event('input', { bubbles: true }));
  el.dispatchEvent(new Event('change', { bubbles: true }));
}
```

---

## 2. Strategi Selektor Element

### Event Detail — `byId`, `byName`, `byLabel` fallback chain

```javascript
function byId(id) { return document.getElementById(id); }
function byName(n) { return document.querySelector(`[name="${n}"]`); }
function byLabel(text) {
  const l = [...document.querySelectorAll('label')].find(l => l.textContent.includes(text));
  return l?.closest('div')?.querySelector('input,select,textarea');
}
```

Pola panggil: `fill(byId('name') || byName('name') || byLabel('Event Name'), DATA.eventName);`

### Timeline — placeholder-based detection

```javascript
const allNameInputs = document.querySelectorAll('input[placeholder*="Early Bird"], input[placeholder*="early bird"]');
const dtInputs = document.querySelectorAll('main input[type="datetime-local"]');
```

### Pricing — category block detection + table

```javascript
function findCategoryBlock(input) {
  let el = input;
  for (let i = 0; i < 10; i++) {
    el = el.parentElement;
    if (!el) return null;
    const hasColor = el.querySelector('input[type="color"]');
    const hasNum = el.querySelector('input[type="number"]');
    const colorCount = el.querySelectorAll('input[type="color"]').length;
    if (hasColor && colorCount === 1 && hasNum) return el;
  }
  return null;
}
```

Kemudian `table.querySelectorAll('tbody tr')` untuk pricing matrix.

### Field — section + children-based detection

```javascript
const sections = document.querySelectorAll('section');
let section = null;
for (const sec of sections) {
  const h = sec.querySelector('h1, h2, h3, h4');
  if (h?.textContent?.trim() === 'Registration Form') { section = sec; break; }
}
const addBtn = [...section.querySelectorAll('button')].find(b => b.textContent?.includes('Add Field'));

// Tiap row field: input[0]=name, input[1]=label, input[2]=type(select)
const rows = section.querySelectorAll('.space-y-3')[0]?.children;
const row = rows[i];
const inputs = row.querySelectorAll('input, select');
```

### Test — Ticket container + person section

```javascript
const tickets = [...document.querySelectorAll('h3')]
  .filter(h => h.textContent.trim() === 'Ticket 1')
  .map(h => h.closest('.bg-white'));

for (let t = 0; t < tickets.length; t++) {
  const grids = tickets[t].querySelectorAll('.grid');
  for (let a = 0; a < grids.length; a++) {
    const fields = [...grids[a].querySelectorAll(':scope > div')];
    // ...
  }
}
```

---

## 3. Pola Umum Script

### Header comment

```javascript
// [Event Name] - [Deskripsi Script]
// Paste in browser console at [halaman]
```

### Console logging

```javascript
console.log('%c═══ [Judul Langkah] ═══', 'color:#6366f1;font-weight:bold');
console.log('✓ [Deskripsi] →', value);
console.warn('⚠ [Warning message]');
console.log('%c═══ Done! [Pesan selesai] ═══', 'color:#22c55e;font-weight:bold;font-size:13px');
```

### Run pattern

```javascript
async function run() {
  // ... logic ...
}
run();
```

---

## 4. Contoh Complete Script

Lihat file referensi di:

```
D:\Hilmi\Coding\OpsMastermind\ScriptTemplate\PrevSuccess\Concert\GMCO\Setup12Apr\
  ├── EventDetail.js    → Step 1: isi form detail event
  ├── Timeline.js       → Step 2: tambah sale timelines
  ├── Pricing.js        → Step 3: kategori + pricing matrix
  ├── Field.js          → Step 4: field registration (4 field)
  ├── Field45.js        → Step 4: field registration (45 field, 5 anggota)
  └── Test45.js         → Step 5: isi sample data
```
